# TP Integrador -- Bot de Telegram en C

Este proyecto implementa un bot de Telegram escrito en **C**, utilizando
un **token** suministrado por el usuario y almacenado en un archivo de
texto.\
El programa se basa exclusivamente en el **código base provisto por la
cátedra**, al cual se le agregaron las funcionalidades solicitadas en el
enunciado del TP.

------------------------------------------------------------------------

## 📌 Funcionalidades

-   Lee el **token del bot** desde un archivo `.txt`.
-   Se comunica con la API de Telegram utilizando **libcurl**.
-   Recibe mensajes enviados al bot y responde automáticamente:
    -   Respuesta al saludo ("hola").
    -   Respuesta a despedida ("chau", "adios").
    -   Respuesta para texto no reconocido.
-   Manejo de archivos según lo especificado en el PDF del TP.
-   No se modificó **ninguna línea del código base**, solo se agregaron
    las funciones solicitadas.

------------------------------------------------------------------------
